import { wordLimit } from 'front/helpers/word-limit';
import { module, test } from 'qunit';

module('Unit | Helper | word limit');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = wordLimit([42]);
  assert.ok(result);
});
