import { short } from 'front/helpers/short';
import { module, test } from 'qunit';

module('Unit | Helper | short');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = short([42]);
  assert.ok(result);
});
